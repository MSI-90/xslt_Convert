<Window x:Class="xslt_GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:xslt_GUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="600"
        MinHeight="250" MinWidth="200" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Файл:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="filePathTextBox" Width="300" Margin="0,0,10,0" 
                 Text="Data1.xml" IsReadOnly="True"/>
            <Button Content="Выбрать файл..." Click="Button_Click" Padding="10,5"/>
        </StackPanel>

        <Button Grid.Row="1" Content="Запустить обработку" Click="Button_Click_1" 
            Padding="10,5" Margin="0,0,0,10"/>

        <DataGrid Grid.Row="2" x:Name="grid" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding Surname}" Width="*"/>
                <DataGridTextColumn Header="Всего" Binding="{Binding Total}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" x:Name="monthlyTotals" FontWeight="Bold" 
               Background="#f0f0f0" Padding="10" TextWrapping="Wrap"/>
    </Grid>
</Window>
